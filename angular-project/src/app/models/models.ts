export interface NavLink {
    label: string;
    link: string;
}

export interface Options {
    id: string;
    value: string;
    img_source?: string;
}

export interface Measurements {
    latimea: number;
    inaltimea: number;
    numarHornuri: number;
}